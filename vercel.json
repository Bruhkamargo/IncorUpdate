{
    "version": 2,
    "builds": [
      { "src": "jsPDF/generated-files/**", "use": "@vercel/static" }
    ],
    "routes": [
        {
            "src": "/[^.]+",
            "dest": "/",
            "status": 200
        }
    ]
}